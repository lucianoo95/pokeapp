import styled from "styled-components";

const MainPhoto = styled.img`
  height: 220px;
  width: 220px;
  object-fit: cover;
  z-index: 111;
  margin-top: -15px;
  filter: drop-shadow(0px 0px 4px #a0bbfa);
`;

export default MainPhoto;
